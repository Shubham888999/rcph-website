{
  "name": "rcph-website-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend build tooling for the RCPH public website.",
  "scripts": {
    "build:css": "tailwindcss -i ./css/tailwind-input.css -o ./css/public-modern.css --minify",
    "watch:css": "tailwindcss -i ./css/tailwind-input.css -o ./css/public-modern.css --watch"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.15",
    "tailwindcss": "^3.4.19"
  },
  "dependencies": {
    "swiper": "^12.2.0"
  }
}
